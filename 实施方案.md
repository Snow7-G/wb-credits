# wb-credits 实施方案

> 版本 v1 · 2026-09-16 · 状态：待评审
> 本文档只描述方案，不含实现代码。

---

## 1. 项目定位

给 WorkBuddy 用户看清积分花在哪里。

WorkBuddy 官网的「套餐与用量」只有总量和逐条 Request 明细，没有"某个对话一共花了多少"这个视图。本项目补上这块，并把消耗按会话、按模型、按异常请求做归因。

**目标用户**：装了 WorkBuddy、关心积分去向、会用斜杠命令的人。

**明确不做**：

- 不做界面挂件。客户端没有第三方 UI 扩展点，MCP App 属内置专属。
- 不给"这个对话最终会花多少"这类点估计。算不出来，写出来就是骗人。
- 不联网、不上传。所有数据留在本地。

---

## 2. 已验证的技术事实

以下均为本机实测结论，实现时直接采用，不必重新试错。

### 2.1 数据源

`~/.workbuddy/workbuddy.db`，SQLite，WAL 模式。

| 表 | 关键字段 | 说明 |
|---|---|---|
| `sessions` | `id` / `title` / `custom_title` / `cwd` / `model` / `created_at` / `updated_at` | 会话元信息 |
| `session_usage` | `session_id` / `used` / `size` / `credit_json` | 用量 |

- `credit_json` 是 `{"请求ID": 积分}` 的逐次明细，求和即该会话总积分。
- `used` / `size` 是上下文 token 占用与窗口大小，**不是积分**。
- `used` 的确切语义（当前值还是峰值）未找到权威说明，输出时只能标为"上下文占用"，不做进一步推断。

### 2.2 会话定位

环境变量直接给出，无需推断：

| 变量 | 用途 |
|---|---|
| `CODEBUDDY_SESSION_ID` | 当前会话 ID，与 `sessions.id` 对应 |
| `CODEBUDDY_CONVERSATION_REQUEST_ID` | 当前这一轮的请求 ID |

已实测核对一致。这是"一键看本对话消耗"能成立的前提。

### 2.3 请求时间戳

数据库不存单次请求时间，需从审计日志回填，而审计日志是**两层写入**：

- `~/.workbuddy/audit-log/YYYY-MM-DD.jsonl`（已归档）
- `~/.workbuddy/audit-log/spool/*.jsonl`（未合并）

**必须两处都读**，否则最新几轮拿不到时间。实测覆盖率：按请求数约 53%，按金额约 90%（有工具调用的请求才进审计日志，而这类请求消耗更大）。

`credit_json` 的键顺序接近写入顺序，可用于给缺时间戳的请求做粗排序，但不可作为精确依据。

### 2.4 输出通道

**这是本项目最关键的约束，决定架构。**

官方 plugin-dev 文档 `command-development/SKILL.md` 第 30 行：

> Commands are written for agent consumption, not human consumption.

斜杠命令的内容是写给 agent 的指令，`!` 注入的 shell 输出也进入 agent 上下文。**插件没有面向用户的直接输出通道**，所有内容由模型转述。

补充实测：

- 对话区**不渲染**模型输出的裸 HTML。测试用的 `<div style="...">` 被原样显示为标签文本。
- 走**渲染工具**输出卡片可以，观感已确认（本会话多轮验证）。
- Hook 的 stdout 能直接进 transcript（不经模型），但形态为纯文本，且位于对话记录而非回复正文。

**结论**：呈现走"模型调用渲染工具"这条路。

### 2.5 成本特性

- 积分**流式累加**，不是请求结束后一次性写入。同一会话隔几十秒查询，数字会变；未结算的请求只能看到部分值。
- 单轮成本**不单调递减**，受两个变量支配：缓存是否命中、这一轮任务有多重。
- **闲置后首轮显著变贵**。实测本会话 12:10 后空闲 2 小时 19 分，14:29 那轮 5.18 积分，是前一轮 0.57 的 **9 倍**（前缀缓存过期，上下文按全价重算）。

第 3 条是唯一可以确定性提示用户的规律，价值高于任何"总消耗预测"。

---

## 3. 功能范围

### MVP（v1）

| 命令 | 行为 |
|---|---|
| `/credits` | 本对话消耗卡片 |
| `/credits --all` | 全部会话排行，按积分降序 |
| `/credits -k <关键词>` | 按会话标题过滤 |
| `/credits --detail` | 展开该会话逐次请求明细 |

### 暂不做

趋势图、按模型归因、离群检测已在实验阶段跑通，但 v1 不带。理由：先验证主链路能不能装、能不能跑，再叠功能。

独立桌面挂件不做。它是另一个项目，与插件形态无关，且分发门槛高。

---

## 4. 架构

三层，脚本与呈现分离。

```
命令 /credits
  → CLI 入口读环境变量定位会话
  → 数据层：只读数据库 + 审计日志（含 spool）
  → 计算层：聚合、边际成本、异常识别
  → 输出 JSON
  → 模型按模板调渲染工具出卡片
```

**为什么脚本与呈现分离**：渲染必须由模型完成（见 2.4），脚本只负责产出结构化数据。这样脚本可被单独调用、单测、复用，不绑定渲染方式。

---

## 5. 目录结构

```
wb-credits/                               仓库根，同时充当插件市场
├── .codebuddy-plugin/
│   └── marketplace.json                  市场清单，plugins[0].source 指向 ./plugins/wb-credits
├── plugins/
│   └── wb-credits/                       插件本体
│       ├── .codebuddy-plugin/
│       │   └── plugin.json               插件清单
│       ├── commands/
│       │   └── credits.md                /credits 命令，写给 agent 的指令
│       └── skills/
│           └── wb-credits/
│               ├── SKILL.md              自然语言触发 + 渲染指令
│               └── scripts/
│                   ├── wb_credits.py     CLI 入口，输出 JSON
│                   ├── data.py           数据层，只读、抗摔
│                   ├── metrics.py        计算层
│                   └── render.py         卡片与纯文本模板
├── tests/
├── README.md
└── 实施方案.md
```

**为什么是两层**：仓库要同时充当插件市场（`marketplace.json` 必须在根），而插件本体要有自己的 `plugin.json`。两者不能放在同一个 `.codebuddy-plugin/` 目录下，否则清单语义冲突。因此市场清单留在根，插件下沉到 `plugins/` 子目录，由 `source` 字段指过去。这也是官方文档推荐的结构。

插件清单必须位于 `.codebuddy-plugin/plugin.json`。`commands/` 与 `skills/` 走默认目录发现，无需在清单里声明。

---

## 6. 输出格式规范

排版已定稿，实现时不得改动结构。

### 布局

左右分栏 + 底部通栏。

| 区域 | 内容 |
|---|---|
| 左栏 | `本对话` / 累计积分（30px 主视觉）/ `积分｜轮次｜上下文` |
| 右栏 | `最近三轮` / 逐轮数值 / `再聊 10 轮｜约 N 积分` |
| 底栏通栏 | 异常提示，格式 `14:29 单轮 5.18（闲置 2h19m）` |

### 硬性约束

- 分隔符统一用全角竖线 `｜`，不用 `·`
- 颜色全部走主题变量，**禁止硬编码色值**：
  - 卡片底 `var(--color-background-secondary)`
  - 正文 `var(--color-text-primary)` / 标签 `var(--color-text-secondary)` / 辅助 `var(--color-text-tertiary)`
  - 分隔线 `0.5px solid var(--color-border-tertiary)`
- 主数字字号 30px、字重 500；标签 12px；正文 15px
- 文案名词化、工具化，不写解释腔
- 不加字间距、阴影、渐变、图标

### 模板骨架

```html
<div style="background:var(--color-background-secondary);border-radius:12px;padding:26px 30px;font-family:var(--font-sans);max-width:620px;">
  <div style="display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);">
    <div style="padding-right:30px;">
      <div style="font-size:12px;color:var(--color-text-secondary);">本对话</div>
      <div style="font-size:30px;font-weight:500;color:var(--color-text-primary);line-height:1.1;margin-top:12px;">{累计积分}</div>
      <div style="font-size:12px;color:var(--color-text-tertiary);margin-top:10px;">积分｜{轮次} 轮｜上下文 {上下文}</div>
    </div>
    <div style="padding-left:30px;border-left:0.5px solid var(--color-border-tertiary);">
      <div style="font-size:12px;color:var(--color-text-secondary);">最近三轮</div>
      <div style="font-size:15px;color:var(--color-text-primary);margin-top:14px;">{逐轮数值，｜分隔}</div>
      <div style="font-size:12px;color:var(--color-text-tertiary);margin-top:12px;">再聊 10 轮｜约 {预估} 积分</div>
    </div>
  </div>
  <div style="font-size:12px;color:var(--color-text-tertiary);margin-top:24px;padding-top:18px;border-top:0.5px solid var(--color-border-tertiary);">{异常提示}</div>
</div>
```

### 降级

渲染失败时退回等宽纯文本版，保持相同的分栏与字段，不加颜色：

```
本对话                            最近三轮
23.99                            1.22 ｜ 1.37 ｜ 0.37
积分 ｜ 16 轮 ｜ 上下文 22 万       再聊 10 轮 ｜ 约 10 积分

14:29 单轮 5.18（闲置 2h19m）
```

---

## 7. 关键实现约定

**数据访问**

- 数据库一律以只读方式打开：`file:...?mode=ro`。客户端可能正在写。
- 路径用 `os.path.expanduser` 展开，不硬编码 `~`。
- 支持 `CODEBUDDY_CONFIG_DIR` / `WORKBUDDY_CONFIG_DIR` 环境变量覆盖默认路径。

**抗摔**

- 表不存在、字段缺失、`credit_json` 解析失败，逐项兜底，给人话提示，不抛堆栈。
- 启动时探测 schema，不匹配则明确报"当前不支持该版本"，不静默算错。
- **算错比报错糟糕**。用户不会核对数字，他会直接信。
- 时间戳拿不到时留空或标注，不要猜一个填上。

**成本计算**

- 边际成本用**最近 N 轮已结算请求**的均值，不用全程均值。
- "再聊 10 轮"必须带前提，措辞挂靠"最近三轮"。
- 区分已结算与进行中：进行中的值标注"未定稿"。

**平台**

- 只用标准库，不引入依赖。
- 兼容 `python3` 与 `python` 两种可执行名。
- 命令文件中用 `${CODEBUDDY_PLUGIN_ROOT}` 定位插件目录，失败时回退到 Glob 搜索。

**渲染稳定性**

- `SKILL.md` 中必须写死"必须调用渲染工具，不得退化为纯文本"，并给出模板骨架。
- 渲染失败的降级路径要写清楚。

---

## 8. 验收标准

全部满足才算跑通。

1. `/plugin marketplace add <本地路径>` → `/plugin install wb-credits@snow7g` → `/reload-plugins`，三条命令无报错。
2. `/credits` 能出卡片，数字与手工查库一致。
3. `/credits --all` 能出排行。
4. 换工作目录、换另一个会话，均正常。
5. 故意破坏数据（删表、改字段名），给出人话提示而非堆栈。
6. **找一个人在其他机器上装一次**，记录他卡在哪一步。

第 6 条是唯一的真验证，前五条只证明本地能跑。

---

## 9. 风险与未决

| 风险 | 影响 | 应对 |
|---|---|---|
| DB schema 随客户端升级变更 | 插件失效 | 启动探测 + 明确报错 + README 标注验证版本 |
| 模型忘记调渲染工具 | 观感退化成纯文本 | skill 指令写死 + 降级模板可接受 |
| 审计日志格式或路径变更 | 时间戳丢失 | 时间戳降级为可选，不影响主功能 |
| 分发依赖用户手动加市场 | 装机量低 | 无法规避，README 把命令写成一行的复制块 |
| 事件定位依赖环境变量 | 变量改名则失效 | 加回退：取 `updated_at` 最新的会话 |

**未决项**

- `used` 字段语义未确认，README 中不得给出确定性解释。
- 是否需要 `--pretty` 开关。当前设计默认就走渲染，若渲染成本被证明偏高，再考虑加开关。
- 是否收录趋势与归因。实验已跑通，等主链路验收后再定。

---

## 10. 里程碑

| 阶段 | 内容 | 完成标志 | 状态 |
|---|---|---|---|
| M1 | 数据层 + CLI，输出 JSON | 命令行能打印本会话数据 | 完成 |
| M2 | 命令 + skill 接入 | `/credits` 在本地装上并出数 | 完成（待真机试装） |
| M3 | 渲染模板 | 卡片效果与第 6 节一致 | 完成 |
| M4 | 抗摔 + 跨平台 | 验收标准第 5 条通过 | 完成（133 项测试通过） |
| M5 | 文档 + 发布 | README 完成，他人试装通过 | 部分（README 完成，待试装） |

每个阶段结束后本地 commit。**跑通 M5 之前不建公开仓库**，避免空仓库开局。

---

## 附：已有可复用资产

| 资产 | 原位置 | 处理 |
|---|---|---|
| 查询脚本 | 工作区 `wb-credits.py` | 已并入 `data.py` 与 `metrics.py` |
| 插件骨架 | 工作区 `wb-credits-plugin/` | 已迁入本目录，补齐数据层与渲染层 |
| 用户级 skill | `~/.workbuddy/skills/wb-credits/` | 装上插件后删除，避免同名冲突 |
